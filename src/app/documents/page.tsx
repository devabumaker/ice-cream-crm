"use client";import {useEffect,useState} from "react";import Link from "next/link";import {FileText} from "lucide-react";import type{Order}from"@/lib/types";const m=(n:number)=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB',maximumFractionDigits:0}).format(n);export default function Documents(){const[o,setO]=useState<Order[]>([]);useEffect(()=>{fetch('/api/orders').then(r=>r.json()).then(setO)},[]);return <div><h1 className="mb-2 text-2xl font-bold">Документы</h1><p className="mb-6 text-sm text-slate-500">Откройте документ и выберите «Печать» — браузер позволит сохранить его в PDF.</p><div className="card overflow-hidden"><table className="data-table"><thead><tr><th>Документ</th><th>Клиент</th><th>Дата доставки</th><th>Сумма</th><th></th></tr></thead><tbody>{o.map(x=><tr key={x.id}><td>Накладная / счёт #{x.id}</td><td>{x.client?.name}</td><td>{x.deliveryDate||'—'}</td><td>{m(x.totalAmount)}</td><td><Link className="btn btn-secondary !px-3" href={`/documents/${x.id}`}><FileText className="h-4 w-4"/>Открыть</Link></td></tr>)}</tbody></table></div></div>}
